import FormProducts from "../formProduct/FormProduct";

const ProductsList = (props: any) => {
  return (
    <div>
      <FormProducts product={false} />
    </div>
  );
};

export default ProductsList;
